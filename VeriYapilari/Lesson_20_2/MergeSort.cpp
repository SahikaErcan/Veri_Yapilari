#include <stdio.h>
#include <stdlib.h>

#define size 8;

void mergeSort(int arr[], int n) {
	int aux[size], i, j, k, L1, L2, _size, u1, u2, tur = 0;
	_size = 1;
	while (_size < n) {
		L1 = 0;
		tur++;
	}
}



int main(){

}